1-
DELIMITER//
Create procedure usid as
@usuario int;
@n int;
select usuario_id from usuarios where usuario_id = @usuario;
end;
declare m int
 set m = 0
while m <= n   do
return select prestamoid from prestamos where usuarioID = @usuario
m++;
end while;

2-
DELIMITER//

create procedure ll as
@nombre varchar(60);
@l int;
set l = 0;
Repeat 
select titulo into nombre from libros where libroid = l
l++;
until nombre repeats "harry potter";
end repeat;
DELIMITER;

3-
DELIMITER//
create procedure contar()
begin
declare contador int;
loopp loop;
select @ from prestamos join usuarios u on usuario_id = u.usuario_id where usuario_id = contador;
contador = contador + 1;
if usuarioID not exist then
leave loopp;
end if;
end loop;
DELIMITER;

4-

create function clasico(Libro_id int)
returns int;
begin
declare clasicos varchar(40);

while 

end while;
